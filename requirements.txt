# NB: `telebot` on PyPI is a *different* project; it only pulled
# pyTelegramBotAPI in as a transitive dependency. Pin the real one.
loguru>=0.7.0,<1.0.0
pyTelegramBotAPI>=4.29,<5.0
mutagen>=1.47
# used directly for streaming Bot API file downloads
requests>=2.31
yt-dlp>=2025.1.1
